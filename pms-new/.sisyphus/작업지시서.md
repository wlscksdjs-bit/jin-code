# PMS-NEW 프로젝트 작업지시서 (v1.0)

**작성일**: 2026-03-22  
**프로젝트**: pms-new  
**목적**: 기존 프로젝트 분석 → 실행 가능하게 수정 → 무결성 테스트 → 고도화

---

## 1. 현재 상태 분석

### 1.1 프로젝트 구조 ✅

| 항목 | 상태 | 비고 |
|------|------|------|
| **Framework** | Next.js 15.5.14 | ✅ 최신 |
| **Database** | SQLite (dev.db) | ⚠️ PostgreSQL 권장 |
| **ORM** | Prisma 7.5.0 | ✅ |
| **Auth** | NextAuth 5 | ✅ |
| **UI** | Radix UI + Tailwind 4 | ✅ |
| **Testing** | Playwright | ✅ |
| **Excel** | xlsx 라이브러리 | ✅ |

### 1.2 데이터 모델 ✅

| 모델 | 상태 | 항목 수 |
|------|------|---------|
| User, Customer | ✅ | 2개 |
| Project, ProjectMember | ✅ | 2개 |
| Sales | ✅ | 1개 |
| Budget, BudgetItem | ✅ | 2개 |
| WbsItem, Progress | ✅ | 2개 |
| CostEstimate, CostEstimateItem | ✅ | 2개 |
| CostExecution, CostExecutionItem | ✅ | 2개 |
| CostActual, CostActualItem | ✅ | 2개 |
| PurchaseOrder, PurchaseOrderItem | ✅ | 2개 |
| MaterialReceipt | ✅ | 1개 |
| CashFlow | ✅ | 1개 |
| MonthlyCostSnapshot | ✅ | 1개 |
| ProjectDashboard | ✅ | 1개 |
| 기타 (Task, Document 등) | ✅ | 8개 |
| **총** | | **30개 모델** |

### 1.3 페이지/라우트 ✅

| 경로 | 상태 | 설명 |
|------|------|------|
| `/` | ⚠️ 미완성 | 루트 페이지 단순 텍스트만 |
| `/signin` | ✅ | 로그인 페이지 |
| `/(dashboard)/` | ✅ | 대시보드 레이아웃 + 사이드바 |
| `/(dashboard)/dashboard-page` | ✅ | KPI 카드, 프로젝트 현황 |
| `/(dashboard)/projects` | ✅ | 프로젝트 목록 |
| `/(dashboard)/projects/[id]` | ✅ | 프로젝트 상세 |
| `/(dashboard)/projects/[id]/wbs` | ✅ | WBS 관리 |
| `/(dashboard)/projects/[id]/schedule` | ✅ | 일정 관리 |
| `/(dashboard)/projects/[id]/cashflow` | ✅ | 자금수지 |
| `/(dashboard)/sales` | ✅ | 영업수주 |
| `/(dashboard)/cost/[projectId]/estimate` | ✅ | 견적원가 |
| `/(dashboard)/cost/[projectId]/execution` | ✅ | 실행원가 |
| `/(dashboard)/cost/[projectId]/monthly` | ✅ | 월별 실적 |
| `/(dashboard)/orders` | ✅ | 발주 목록 |
| `/(dashboard)/orders/[id]` | ✅ | 발주 상세 |
| `/(dashboard)/receipts` | ✅ | 입고 목록 |
| `/(dashboard)/vendors` | ✅ | 거래처 |
| `/(dashboard)/budget` | ✅ | 예산 관리 |

### 1.4 Server Actions ✅

| 파일 | 상태 | 함수 수 |
|------|------|---------|
| `projects.ts` | ✅ | CRUD, 목록, 집계 |
| `sales.ts` | ✅ | 영업 CRUD |
| `cost-estimates.ts` | ✅ | 견적원가 CRUD |
| `cost-executions.ts` | ✅ | 실행원가 CRUD |
| `purchase-orders.ts` | ✅ | 발주 CRUD |
| `material-receipts.ts` | ✅ | 입고 CRUD |
| `budgets.ts` | ✅ | 예산 CRUD |
| `cash-flow.ts` | ✅ | 자금흐름 CRUD |
| `dashboard.ts` | ✅ | KPI 계산, 집계 |
| `wbs.ts` | ✅ | WBS CRUD |
| `sheets.ts` | ✅ | Google Sheets 연동 |
| `drive.ts` | ✅ | Google Drive 연동 |

### 1.5 컴포넌트 ✅

| 컴포넌트 | 상태 | 설명 |
|----------|------|------|
| `gantt-chart.tsx` | ✅ | 간트차트 |
| `dashboard-alerts.tsx` | ✅ | 알림 표시 |
| `theme-toggle.tsx` | ✅ | 다크/라이트 모드 |
| `use-sse.ts` | ✅ | SSE 클라이언트 훅 |
| `ui/*` | ✅ | shadcn/ui 기본 컴포넌트 |

---

## 2. 발견된 문제점

### 2.1 🚨 Critical (즉시 수정 필요)

| # | 문제 | 영향 | 위치 |
|---|------|------|------|
| 1 | **의존성 손상** | npm install 실패, 빌드 불가 | package.json |
| 2 | **루트 페이지 미완성** | `/` 경로가 단순 텍스트만 표시 | `app/page.tsx` |
| 3 | **notifications 페이지 없음** | 사이드바 링크가 동작 안함 | - |
| 4 | **budgets API 누락** | Server Action에 budget 관련 함수 없음 | `actions/budgets.ts` |

### 2.2 ⚠️ Warning (수정 권장)

| # | 문제 | 영향 | 위치 |
|---|------|------|------|
| 5 | 프로젝트 상세 페이지에서 `projectId` 타입 오류 가능 | 빌드 경고 | `projects/[id]/page.tsx` |
| 6 | Dashboard에서 역할 표시 한국어/한자 혼용 | UI 불일치 | `dashboard-page.tsx:44` |
| 7 | E2E 테스트 파일 없음 | 품질 보증 불가 | `e2e/` |

### 2.3 📝 Minor (향후 개선)

| # | 항목 | 설명 |
|---|------|------|
| 8 | SSE 실시간 업데이트 미구현 | use-sse 훅만 존재 |
| 9 | Google Sheets/Drive 연동 미완성 | actions만 존재 |
| 10 | Excel 내보내기 미구현 | xlsx 라이브러리 설치됨 |
| 11 | 문서화 부족 | README가 기본 템플릿 |

---

## 3. 수정 작업 (Phase 1: 기본 실행 가능 상태)

### Task 1-1: 루트 페이지 수정

**파일**: `src/app/page.tsx`

**현재 문제**: 단순 텍스트만 표시
```tsx
export default function Home() {
  return (
    <main style={{ padding: '40px', textAlign: 'center' }}>
      <h1>PMS 2.0</h1>
      <p>프로젝트 관리 시스템</p>
    </main>
  )
}
```

**수정方案**: 대시보드로 리다이렉트
```tsx
import { redirect } from 'next/navigation'
import { auth } from '@/lib/auth'

export default async function Home() {
  const session = await auth()
  if (session) {
    redirect('/')
  }
  redirect('/signin')
}
```

**QA Scenarios**:
```
Scenario: 루트 페이지 접근
  Tool: Playwright
  Steps:
    1. http://localhost:3000 접속
    2. 로그인 상태 확인
  Expected Result: 대시보드로 리다이렉트 또는 로그인 페이지
  Evidence: .sisyphus/evidence/task-1-1-redirect.png
```

---

### Task 1-2: Notifications 페이지 생성

**파일**: `src/app/(dashboard)/notifications/page.tsx`

**생성해야 할 내용**:
- 알림 목록 표시 (Notification 모델)
- 읽음/안읽음 필터
- 일괄 읽음 처리
- 알림 클릭 → 관련 페이지 이동

**QA Scenarios**:
```
Scenario: 알림 페이지 렌더링
  Tool: Playwright
  Steps:
    1. 로그인 후 /notifications 접속
    2. 알림 목록 확인
  Expected Result: 알림 목록 정상 표시
  Evidence: .sisyphus/evidence/task-1-2-notifications.png
```

---

### Task 1-3: Budgets Server Action 완성

**파일**: `src/app/actions/budgets.ts`

**현재 상태**: 파일 존재 확인 필요

**추가해야 할 함수**:
- `getBudgets(projectId: string)`: 프로젝트별 예산 목록
- `createBudget(data)`: 예산 생성
- `updateBudget(id, data)`: 예산 수정
- `deleteBudget(id)`: 예산 삭제
- `convertEstimateToBudget(projectId, estimateId)`: 견적→예산 전환

---

### Task 1-4: 의존성 재설치 및 빌드 검증

**실행 명령**:
```bash
cd pms-new
rm -rf node_modules package-lock.json
npm install
npx prisma generate
npm run build
```

**QA Scenarios**:
```
Scenario: 빌드 성공
  Tool: Bash
  Steps:
    1. npm run build
  Expected Result: 빌드 성공, 에러 없음
  Evidence: .sisyphus/evidence/task-1-4-build.log
```

---

## 4. 무결성 테스트 (Phase 2)

### Task 2-1: 단위 테스트 작성

**대상**: Server Actions

```typescript
// tests/unit/projects.test.ts
import { describe, it, expect } from 'vitest'
import { listProjects, createProject } from '@/app/actions/projects'

describe('Project Actions', () => {
  it('should list projects', async () => {
    const projects = await listProjects()
    expect(Array.isArray(projects)).toBe(true)
  })
})
```

---

### Task 2-2: E2E 테스트 작성

**파일**: `e2e/app.spec.ts`

```typescript
import { test, expect } from '@playwright/test'

test.describe('PMS 2.0 E2E', () => {
  test('should login and view dashboard', async ({ page }) => {
    await page.goto('/signin')
    await page.fill('[name="email"]', 'admin@test.com')
    await page.fill('[name="password"]', 'password')
    await page.click('button[type="submit"]')
    await expect(page).toHaveURL('/')
    await expect(page.locator('h1')).toContainText('대시보드')
  })
})
```

---

### Task 2-3: API Endpoint 테스트

```bash
# 테스트 명령
curl http://localhost:3000/api/projects  # 프로젝트 목록
curl http://localhost:3000/api/dashboard  # 대시보드 데이터
```

---

## 5. 고도화 작업 (Phase 3)

### Task 3-1: SSE 실시간 업데이트 구현

**파일**: 
- `src/app/api/events/route.ts` (SSE endpoint)
- `src/components/use-sse.ts` (훅 완성)

**구현 내용**:
- 실행원가 변경 시 SSE 브로드캐스트
- 대시보드 자동 갱신
- 재연결 로직

---

### Task 3-2: Excel 내보내기 기능

**파일**: `src/lib/excel-export.ts`

**구현 내용**:
- 프로젝트 목록 → Excel
- 견적원가 → Excel (실행예산서 양식)
- 실행원가 → Excel
- 자금수지 → Excel

---

### Task 3-3: Google Sheets/Drive 연동 완성

**파일**: `src/app/actions/sheets.ts`, `src/app/actions/drive.ts`

**구현 내용**:
- 견적원가 → Google Sheets 동기화
- 실행원가 → Google Sheets 동기화
- 문서 → Google Drive 업로드

---

### Task 3-4: 문서화

**파일**: `README.md` 업데이트

**내용**:
- 프로젝트 소개
- 주요 기능
- 설치/실행 방법
- API 문서
- 데이터 모델 문서

---

## 6. 실행 순서

```
Phase 1: 기본 실행 가능 상태 (1-2일)
├── Task 1-1: 루트 페이지 수정
├── Task 1-2: Notifications 페이지 생성
├── Task 1-3: Budgets Server Action 완성
├── Task 1-4: 의존성 재설치 및 빌드 검증
└── → 개발 서버 실행 확인

Phase 2: 무결성 테스트 (1-2일)
├── Task 2-1: 단위 테스트 작성
├── Task 2-2: E2E 테스트 작성
├── Task 2-3: API Endpoint 테스트
└── → 모든 테스트 통과 확인

Phase 3: 고도화 (1주)
├── Task 3-1: SSE 실시간 업데이트
├── Task 3-2: Excel 내보내기
├── Task 3-3: Google Sheets/Drive 연동
└── Task 3-4: 문서화
```

---

## 7. 성공 기준

### Phase 1 완료 기준
- [ ] `npm run dev` 정상 실행
- [ ] `/signin` → 로그인 → 대시보드 정상 표시
- [ ] 모든 사이드바 메뉴 정상 동작
- [ ] 빌드 에러 없음

### Phase 2 완료 기준
- [ ] 단위 테스트 80% 이상 커버리지
- [ ] E2E 테스트 주요 플로우 통과
- [ ] API 응답 정상

### Phase 3 완료 기준
- [ ] SSE 실시간 업데이트 동작
- [ ] Excel 내보내기/가져오기 가능
- [ ] Google 연동 동작
- [ ] README 문서 완성

---

## 8. 참고 자료

### 기존 분석 파일
- `실행예산서.xlsx` - 실제 양식 참고
- `실행예산서 갑지.xlsx` - 갑지 양식
- `src/lib/excel-template.ts` - 기존 템플릿 코드

### 관련 스킬
- nextjs-best-practices
- playwright-testing
- tailwind-v4-shadcn

---

**최종 업데이트**: 2026-03-22
**버전**: 1.0
**상태**: 계획 완료, 실행 대기
